#inputs needed

#site_name = 
#in a larger lab topology this would then be translated to find the devices at the site
#the current lab topology is to small snf not setup to support this at the moment 

customer_number = 1006
# mapped to an extended range vlan (1006 - 4094) for this example

internal_asn = 1000
#1000 is what is used in the lab as it is single ASN

customer_subnet = "192.168.123.0/24"
#subnet customer wants to provision in CIDR notation

